language: python
sudo: false
dist: xenial
notifications:
    email: change

python:
    - 3.6.5
    - 3.7.1

env:
    - LINTING=1
    - NUMBA=0 COVERAGE=1
    - NUMBA=1
    - DOCTEST=1

before_install:
    - python -m pip install --upgrade pip
    - python -m pip install pytest>=3.6
    - if [ "${LINTING}" == "1" ]; then
          pip install flake8;
      fi
    - if [ "${COVERAGE}" == "1" ]; then
          pip install coverage coveralls codecov pytest-cov;
      fi

install:
    - if [ "${NUMBA}" == "1" ]; then
          pip install -e .[numba];
      elif [ "${DOCTEST}" == "1" ]; then
          pip install -e .[doc];
      else
          pip install -e .;
      fi

script:
    - |
      if [ "${LINTING}" == "1" ]; then
          flake8 netneurotools;
      elif [ "${DOCTEST}" == "1" ]; then
          cd docs;
          make html;
          make doctest;
      else
          if [ "${COVERAGE}" == "1" ]; then
              TEST_ARGS="--cov-report term-missing --cov=netneurotools";
          fi
          py.test ${TEST_ARGS} netneurotools;
      fi

after_success:
    - if [ "${COVERAGE}" == "1" ]; then
          coveralls; codecov;
      fi
